<table class="table table-striped table-bordered">
    <tbody>
        <tr>
            <th>Name</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
        {{#each model as |workout|}}
            <tr class="table-striped">
                <td>
                    {{workout.name}}
                </td>
                <td>
                    {{#link-to 'workout.edit' workout tagName="button" class="btn" }}Edit{{/link-to}}
                </td>
                <td>
                    <button class="btn" {{action 'delete' workout.id}}>Delete</button>
                </td>
            </tr>
            {{else}}
                <tr class="table-striped">
                    <td></td>
                    <td></td>
                    <td></td>
                </tr>
        {{/each}}
    </tbody>
</table>
{{#link-to 'workout.create' tagName="button" class="btn" }}Create{{/link-to}}

